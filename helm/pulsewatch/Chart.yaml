apiVersion: v2
name: pulsewatch
description: PulseWatch service monitoring platform (API, worker, PostgreSQL) behind Envoy Gateway
type: application
# Chart version — bump on chart changes
version: 0.1.0
# Application version — the PulseWatch image version this chart deploys by default
appVersion: "1.0.8"
